function smallShop(input) {
    let product = input[0];
    let place = input[1];
    let quantity = input[2];
    let coffee = 0;
    let water = 0;
    let beer = 0;
    let sweets = 0;
    let peanuts = 0;
    let totalSum = 0;

    if (place == "Sofia") {
        coffee = 0.5;
        water = 0.8;
        beer = 1.20;
        sweets = 1.45;
        peanuts = 1.60;
        totalSum = product
    }
}